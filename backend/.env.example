APP_NAME=Laravel
APP_ENV=production
APP_KEY=base64:5Q3ZApHhK7A9+4f3hFnIiLOcwZoLp3FqvvO1lGiNj40=
APP_DEBUG=false
APP_URL=https://laravel-react-ecommerce-project-production.up.railway.app

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# --- Stripe settings ---
# If your DB prices are in VND but you want to charge USD,
# set a conversion rate (1 VND → USD), e.g., 0.00004
PRICE_CONVERT_VND_TO_USD_RATE=

# Database Configuration
# For MySQL (Railway built-in):
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=railway
DB_USERNAME=root
DB_PASSWORD=

# For PostgreSQL (Neon or Railway Postgres):
# Uncomment these lines and comment out MySQL lines above
# DB_CONNECTION=pgsql
# DB_HOST=your-neon-host.neon.tech
# DB_PORT=5432
# DB_DATABASE=neondb
# DB_USERNAME=your-username
# DB_PASSWORD=your-password
# DATABASE_URL=postgresql://user:pass@host:5432/dbname?sslmode=require

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Stripe (test mode)
STRIPE_SECRET=your_stripe_secret_key
STRIPE_CURRENCY=usd

# Frontend URL for CORS
FRONTEND_URL=https://your-frontend-url.vercel.app
